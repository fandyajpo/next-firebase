export enum ECol {
  Portofolio = "PORTOFOLIO",
  Role = "ROLE",
  Tag = "TAG",
  User = "USER",
}

export enum EQKey {
  Portofolio = "Portofolio",
  Role = "Role",
  Tag = "Tag",
  User = "User",
}

export enum EPermission {
  ADMINISTRATOR = 1,
  MEMBER = 0,
}
